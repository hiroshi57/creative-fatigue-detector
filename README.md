# 広告クリエイティブ 疲弊チェック — 差し替え時期を数字で判断

日別の表示回数とクリック数を入れると、クリエイティブが飽きられているかを判定し、差し替え時期を出します。

## 特徴
- 実データ入力 → 実計算（LLM API不要・キー不要）
- 入力内容は自動保存、JSONで書き出し/読み込み可能
- 非エンジニア向けUI

## 使い方
`frontend/standalone.html` をブラウザで開くだけで動きます。
